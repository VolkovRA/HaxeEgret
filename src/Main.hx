package;

import egret.*;

class Main
{
	static function main()
	{
		// egret:
		trace(IHashObject);
		trace(HashObject);
		trace(Event);
		trace(IEventDispatcher);
		trace(EventDispatcher);
		trace(EventPhase.AT_TARGET);
		
		
	}
}